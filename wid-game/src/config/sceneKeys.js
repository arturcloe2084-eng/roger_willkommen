// Scene key constants — used by all scenes to avoid hard-coded strings.
// Must match the string passed to super() in each Scene constructor.
export const SCENE_KEYS = Object.freeze({
    BOOT: 'BootScene',
    MAIN_MENU: 'MainMenuScene',
    SCENE_ENGINE: 'SceneEngineScene',
    DIALOG: 'DialogScene',
    CROSSWORD: 'CrosswordScene',
    QUIZ: 'QuizScene',
    SIGNAL_LOCATOR: 'SignalLocatorScene',
    GAME_HUD: 'GameHudScene',
    DICTIONARY: 'DictionaryScene',
});
